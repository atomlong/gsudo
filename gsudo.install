post_install() {
    [ -h /usr/bin/sudo ] || {
    MSYS="winsymlinks:lnk" ln -sf "${MSYSTEM_PREFIX}/bin/gsudo" ${MSYSTEM_PREFIX}/bin/sudo
    }
}

post_upgrade() {
  post_install
}

post_remove() {
  [ -h ${MSYSTEM_PREFIX}/bin/sudo ] && rm -vf ${MSYSTEM_PREFIX}/bin/sudo
}
